name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - ai-rebuild-critical

permissions:
  contents: write

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    timeout-minutes: 20

    defaults:
      run:
        working-directory: learn-up

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: learn-up/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Apply chat state migration
        working-directory: .
        run: |
          if [ -f scripts/fix-chat-state.mjs ]; then
            node scripts/fix-chat-state.mjs
            if ! git diff --quiet -- learn-up/src/components/AIChatComponent.tsx learn-up/src/components/JournalSidebar.tsx learn-up/src/components/RecipeSidebar.tsx learn-up/src/components/NotebookWhiteboard.tsx learn-up/src/actions/ai-tutor.ts; then
              git config user.name "learn-up-ci"
              git config user.email "actions@github.com"
              git add learn-up/src/components/AIChatComponent.tsx learn-up/src/components/JournalSidebar.tsx learn-up/src/components/RecipeSidebar.tsx learn-up/src/components/NotebookWhiteboard.tsx learn-up/src/actions/ai-tutor.ts
              git commit -m "fix(ai): wire chat state and interactive panels"
              git push
            fi
          fi

      - name: Apply attachment persistence repair
        working-directory: .
        run: |
          if [ -f scripts/fix-attachments-persistence.mjs ]; then
            node scripts/fix-attachments-persistence.mjs
            if ! git diff --quiet -- learn-up/src/components/AIChatComponent.tsx; then
              git config user.name "learn-up-ci"
              git config user.email "actions@github.com"
              git add learn-up/src/components/AIChatComponent.tsx
              git commit -m "fix(chat): persist attachments before indexing"
              git push
            fi
          fi

      - name: Apply critical AI finalization
        working-directory: .
        run: |
          if [ -f scripts/fix-critical-ai.mjs ]; then
            node scripts/fix-critical-ai.mjs
            if ! git diff --quiet -- learn-up/src/components/AIChatComponent.tsx learn-up/src/components/JarvisGlobalWidget.tsx learn-up/src/actions/jarvis.ts learn-up/src/actions/ai-tutor.ts learn-up/src/lib/ai/agent-registry.ts learn-up/src/lib/ai/agent-runner.ts; then
              git config user.name "learn-up-ci"
              git config user.email "actions@github.com"
              git add learn-up/src/components/AIChatComponent.tsx learn-up/src/components/JarvisGlobalWidget.tsx learn-up/src/actions/jarvis.ts learn-up/src/actions/ai-tutor.ts learn-up/src/lib/ai/agent-registry.ts learn-up/src/lib/ai/agent-runner.ts
              git commit -m "fix(ai): finalize tool routing, panel access and chat state"
              git push
            fi
          fi

      - name: Repair lint blockers
        working-directory: .
        run: |
          node scripts/fix-lint-blockers.mjs
          if ! git diff --quiet -- learn-up/src/app/legal/page.tsx learn-up/src/lib/ai-tools.ts; then
            git config user.name "learn-up-ci"
            git config user.email "actions@github.com"
            git add learn-up/src/app/legal/page.tsx learn-up/src/lib/ai-tools.ts
            git commit -m "fix(ci): repair lint blockers"
            git push
          fi

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Test
        run: npm test

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: placeholder
          NEXT_PUBLIC_SITE_URL: https://learn-up-qmgx.onrender.com
          AI_PROVIDER: gemini
